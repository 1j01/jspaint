/**
 * Canvas-related constants
 * Default canvas dimensions match Windows XP MS Paint
 */

/**
 * Default canvas width (Windows XP MS Paint default)
 */
export const DEFAULT_CANVAS_WIDTH = 512;

/**
 * Default canvas height (Windows XP MS Paint default)
 */
export const DEFAULT_CANVAS_HEIGHT = 384;

/**
 * Minimum canvas dimensions
 */
export const MIN_CANVAS_WIDTH = 1;
export const MIN_CANVAS_HEIGHT = 1;

/**
 * Maximum canvas dimensions
 */
export const MAX_CANVAS_WIDTH = 5000;
export const MAX_CANVAS_HEIGHT = 5000;
